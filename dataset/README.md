# Dataset

This folder contains the dataset used with the ontology (CSV/Excel/etc.).

The dataset supports examples such as sensor readings (temperature, humidity, soil moisture), timestamps, device IDs, and any sample records referenced in the report.
